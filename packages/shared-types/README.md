# @assem/shared-types

Shared contracts for messages, providers, tools, policies, sessions, and transport payloads. Other packages depend on these types so core behaviors can evolve without coupling to one runtime or integration.
